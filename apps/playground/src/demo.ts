/**
 * A ~2000-character single document, richly cross-linked to specific anchors
 * inside itself. Links use the wrapped-ref form:
 * `<!--lmd:ref targets-->source text<!--/lmd-->` — one ref may point at several
 * anchors at once.
 */
export const DEMO = [
  "# How a compiler works <!--lmd:a overview-->",
  "",
  "A compiler turns source code into a program a machine can run. It works in",
  "stages, each feeding the next. The **front end** understands your language;",
  "the **back end** targets a machine. Between them sits an",
  "<!--lmd:ref :ir-->intermediate representation<!--/lmd--> that keeps the two halves",
  "independent, so you can reuse the <!--lmd:ref :optimizer-->optimizer<!--/lmd--> and",
  "<!--lmd:ref :codegen-->code generator<!--/lmd--> across many languages.",
  "",
  "In one sentence, <!--lmd:ref covers=:lexer,:parser,:sema,:codegen-->source becomes",
  "tokens, a tree, checks, an IR, then machine code<!--/lmd--> — that whole chain is",
  "what the rest of this page walks through.",
  "",
  "## Lexing <!--lmd:a lexer-->",
  "",
  "The lexer — or scanner — reads raw characters and groups them into *tokens*:",
  "keywords, identifiers, numbers, punctuation. It discards whitespace and",
  "comments. Its output is a flat stream the <!--lmd:ref :parser-->parser<!--/lmd-->",
  "can consume without ever worrying about individual characters again.",
  "",
  "## Parsing <!--lmd:a parser-->",
  "",
  "The parser takes the token stream from the <!--lmd:ref :lexer-->lexer<!--/lmd--> and",
  "builds an <!--lmd:ref :ast-->abstract syntax tree<!--/lmd--> by following the",
  "language grammar. When the tokens do not fit that grammar, this is exactly where",
  "a syntax error is raised.",
  "",
  "## The syntax tree <!--lmd:a ast-->",
  "",
  "The abstract syntax tree is your program as structured data: an expression",
  "becomes a node with child nodes. Every later stage walks this tree.",
  "<!--lmd:ref refines=:sema-->Semantic analysis<!--/lmd--> annotates it, and *lowering*",
  "turns it into the <!--lmd:ref :ir-->intermediate representation<!--/lmd-->.",
  "",
  "## Semantic analysis <!--lmd:a sema-->",
  "",
  "Semantic analysis checks what a grammar cannot: are names declared, do types",
  "line up, is a `break` really inside a loop. It decorates the",
  "<!--lmd:ref :ast-->AST<!--/lmd--> with types and resolved symbols, and rejects",
  "programs that <!--lmd:ref :parser-->parse<!--/lmd--> cleanly yet make no sense.",
  "",
  "## Intermediate representation <!--lmd:a ir-->",
  "",
  "The IR is a simpler, machine-independent form of the program. Because both the",
  "<!--lmd:ref part_of=:overview-->front end<!--/lmd--> and the",
  "<!--lmd:ref :optimizer-->optimizer<!--/lmd--> agree on this one shape, you can",
  "support many source languages and many target machines.",
  "",
  "## Optimization <!--lmd:a optimizer-->",
  "",
  "The optimizer rewrites the <!--lmd:ref :ir-->IR<!--/lmd--> to be faster or smaller",
  "without changing what the program computes — folding constants, deleting dead",
  "code, inlining calls. It runs right before <!--lmd:ref :codegen-->code",
  "generation<!--/lmd-->.",
  "",
  "## Code generation <!--lmd:a codegen-->",
  "",
  "Code generation walks the optimized <!--lmd:ref :ir-->IR<!--/lmd-->, emits",
  "instructions for a real machine, assigns registers, and lays out memory. What",
  "comes out is something the CPU can finally run — the whole",
  "<!--lmd:ref :overview-->pipeline<!--/lmd--> in one artifact.",
].join("\n");
