{
  "name": "@lmd/viewer",
  "version": "0.1.0",
  "description": "Renderer for Linked Markdown with a link-graph overlay.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@lmd/core": "workspace:*",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
